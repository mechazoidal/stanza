# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'stanza' do
  # developer('FIX', 'FIX@example.com')

  # self.rubyforge_name = 'stanzax' # if different than 'stanza'
end

# vim: syntax=ruby
